export interface Socio {
  id: number;
  nome: string;
  // Outras propriedades, se houver:
  // cargo?: string;
  // dataAdmissao?: string;
  // Você também pode incluir uma propriedade para a empresa de forma resumida, se precisar:
  // empresa?: Empresa;
}
